<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link  href="style.css" rel="stylesheet">
    <title>Vividio || <%= title %> </title>
</head>
<body style="padding: 0 2%;">
    <div class="navbar">
        <ul>
           <li><a href="/" > Movies</a></li>
           <li><a href="/About"> About</a></li>
           <li><a href="/new_movie"> Add New</a></li>
       </ul>
   </div>
    <div class="movies" >
        <h3 class="heading">Trending Movies</h3>
        <% if (movies.length < 1) { %>
            <p class=" none"> No movies yet, check back later</p>
        <% }
        else {
            %> <% movies.forEach(movies => { %>
                <div style=" background-color: black;padding: 5%;">
                    <a class="single" href="/<%=movies.id %>">
                        <div class="pica">
                            <img src="<%=movies.poster_path%>" alt="" class="img">
                        </div>
                        <div class="texts">
                            <h2 class="title" > Title: <%= movies.title %></h2>
                            <p >Release_date: <%= movies.release_date %></p>
                            <p >Original_language: <%= movies.original_language %></p>
                            <p class="snippet" > Popularity: <%= movies.popularity %></p>
                            <h3 >overview:</h3>
                            <article>
                                <%= movies.overview %>
                            </article>
                        </div>
                    </a>
                    
                </div>
                <hr >
        <% }) %> <%
        } %> 
    </div>
</body>
</html>